#include "main.h"

/**
* mul - a function that multiplies two integers.
* @a: int type number
* @b: int type number
* Return: Always return 0 success and 0 otherwise
*/

int mul(int a, int b)
{
return (a * b);
}
